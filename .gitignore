# Dependencies
node_modules/
yarn.lock
pnpm-lock.yaml

# Build outputs
dist/
build/
*.local

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Temporary files
*.tmp
*.temp
.cache/
.vercel

# DO->SO linking artifacts (regenerable)
do-so-link-checkpoint.json
do-so-link-preview.json
public/accurate-data.backup-*.json

# Accurate API export snapshots (49 MB+ raw data, regenerable via scripts)
accurate-raw.json
accurate-export.json
accurate-fields-debug.json

# Source PDF/xlsx exports used for reconciliation (42 MB+, local only)
Dataset/

# accurate-data.json backups created during reconciliation
public/accurate-data.json.bak
public/accurate-data.json.bak-*

# Firebase deploy cache
.firebase/

# Dataset backups created by reconciliation scripts (repo root)
/accurate-data.bak-*.json

# Large brand mockup (not needed by build; icons generated from "Logo NSA 2.0.png")
Logo NSA 2.0 Full.png

# Firebase Admin service account key (full admin access — NEVER commit)
serviceAccount.json
serviceAccount*.json

# Local DB backup export — never commit (may contain business data)
backup-nusantara.json
